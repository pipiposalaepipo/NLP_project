# NLP_logic/analyze_logic.py
# ในไฟล์ analyze_logic.py
from Call_API import GeminiManager  # เอาจุดออก


# 1. ตั้งค่า API Key (ดึงจาก Environment Variable จะปลอดภัยกว่า)
API_KEY = "AIzaSyCnpJ3c5F4o_FucVaXI-YoM5bTsJsRy2jk"
ai_assistant = GeminiManager(API_KEY)

def analyze_resume_data(resume_text):
    # สร้าง Prompt แยกไว้ที่นี่ จะแก้จะเพิ่มเงื่อนไขก็ง่าย
    prompt = f"""
    คุณคือผู้เชี่ยวชาญด้าน HR 
    วิเคราะห์ Resume นี้และสรุปออกมาเป็น JSON เท่านั้น:
    - Name: ชื่อผู้สมัคร
    - Skills: รายการทักษะ
    - Summary: สรุปความสามารถใน 1 ประโยค
    
    ข้อมูล Resume:
    {resume_text}
    """
    
    # ส่ง Prompt ไปที่ไฟล์ gemini_client
    result = ai_assistant.send_request(prompt)
    return result

# --- ทดสอบรัน ---
if __name__ == "__main__":
    test_text = "Yumin, Software Developer, Python, SQL, NLP Experience"
    print("กำลังวิเคราะห์...")
    print(analyze_resume_data(test_text))